import React from 'react'

export const UserPanel = ({name, email, password}) => {
    return (
        <div>
            asdsads
            User panel
            <p>{name}</p>
            <p>{password}</p>
        </div>
    )
}
